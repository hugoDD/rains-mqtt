<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://maven.apache.org/POM/4.0.0"
	 xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<!--POM-->
	<parent>
		<groupId>com.rains</groupId>
		<artifactId>rains-mqtt</artifactId>
		<version>${rains-mqtt.version}</version>
		<relativePath>../pom.xml</relativePath>
	</parent>
	<!--模块-->
	<artifactId>rains-mqtt-broker</artifactId>
	<!--依赖-->
	<dependencies>
		<dependency>
			<groupId>com.rains</groupId>
			<artifactId>rains-mqtt-common</artifactId>
			<version>${rains-mqtt.version}</version>
		</dependency>
		<dependency>
			<groupId>com.rains</groupId>
			<artifactId>rains-mqtt-store</artifactId>
			<version>${rains-mqtt.version}</version>
		</dependency>
		<dependency>
			<groupId>com.rains</groupId>
			<artifactId>rains-mqtt-auth</artifactId>
			<version>${rains-mqtt.version}</version>
		</dependency>
		<dependency>
			<groupId>io.netty</groupId>
			<artifactId>netty-handler</artifactId>
		</dependency>
		<dependency>
			<groupId>io.netty</groupId>
			<artifactId>netty-codec-mqtt</artifactId>
		</dependency>
		<dependency>
			<groupId>io.netty</groupId>
			<artifactId>netty-codec-http</artifactId>
		</dependency>
		<dependency>
			<groupId>io.netty</groupId>
			<artifactId>netty-transport-native-epoll</artifactId>
			<classifier>linux-x86_64</classifier>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-configuration-processor</artifactId>
		</dependency>
		<dependency>
			<groupId>com.alipay.sofa</groupId>
			<artifactId>bolt</artifactId>
			<version>${bolt.version}</version>
		</dependency>
	</dependencies>
	<!--构建-->
	<build>
		<!--插件-->
		<plugins>
			<!--执行包插件-->
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
				<version>${spring-boot.version}</version>
				<configuration>
					<!--使用devtools时需要配置-->
					<fork>true</fork>
				</configuration>
				<executions>
					<execution>
						<goals>
							<!--可以把依赖的包都打包到生成的Jar包中-->
							<goal>repackage</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>
</project>
